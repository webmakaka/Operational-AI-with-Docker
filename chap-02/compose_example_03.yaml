# Reference only. No demo in Chapter 2; Compose demo in Chapter 3.
services:
  app:
    build: .
    models:
      llm:
        endpoint_var: MODEL_URL
    environment:
      OPENAI_BASE_URL: ${OPENAI_BASE_URL}
      OPENAI_MODEL: ${OPENAI_MODEL}
      MODEL_URL: ${MODEL_URL}
